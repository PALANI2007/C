#include<stdio.h>
int squre(num);
int main()
{
    int num,sum;
    printf("Enter the number:",num);
    scanf("%d",&num);
    sum = square(num);
    printf("Square of the value is:",sum);
    int squre(num)

    }
    int squre(num)
    {
        return num*num;
    }
